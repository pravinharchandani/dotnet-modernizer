# dotnet-modernizer

A Claude Code plugin that analyzes legacy .NET Framework solutions and estimates migration effort to .NET 10. It combines a C# MCP server (Roslyn-based semantic analysis) with subagents, slash commands, and hooks to inventory projects, detect incompatible APIs, and produce structured migration effort reports.
